NODE7 UI includes the user-supplied NODE7 Development Studios logo as:
html/images/node7-logo.png

Future resources may provide complete image URLs or item names. By default,
item names resolve to:
https://cfx-nui-node7-inventory/html/images/<item>.png

The UI always falls back to a text monogram when an item image is missing.
